---
description: 将当前对话压缩为交接文档，让另一个 agent（或未来会话）能无缝接续工作。
---

撰写一份交接文档，总结当前对话内容，使全新的 agent 能继续工作。保存到用户操作系统的临时目录 — 不要放在工作区内。

文档中包含"建议技能"小节，推荐下一个 agent 应调用的 workflow。

不要重复已在其他制品中记录的内容（PRD、计划、ADR、issue、commit、diff）。改用路径或 URL 引用。

脱敏所有敏感信息，如 API Key、密码、个人身份信息。

如果用户传了参数，将其视为下一次会话的聚焦方向，据此裁剪文档内容。
